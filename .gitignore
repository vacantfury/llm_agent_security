# Task text is personal (psyche task standard — all task files gitignored)
TODO.md

# Outputs, data, tracking (large / local)
outputs/
data/
mlruns/
multirun/

# Paper dir: drafts, build artifacts (PDF/docx/zip), author kits, bib + downloaded
# PDFs — all local; this harness repo is not the paper's home / submission repo.
paper/

# Cluster access/property docs — SSH hosts, usernames, SLURM accounts (never public).
# Mirrors of the sibling's canonical cluster_files/ (see cluster_files/README.md).
cluster_files/

# Reviews / ARR / reviewer text — never committed (public repo)
text_docs/reviews/

# Secrets (defense-in-depth; the SessionStart hook mirrors these to .cursorignore)
.env
.env.*
*.env
keys/
secrets/
*.key
*.pem
*credentials*
*.secret

# Python / uv
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Fonts (CJK/Devanagari renders — large, local)
fonts/

# OS / editor
.DS_Store
.idea/
.vscode/

# LaTeX build artifacts
*.aux
*.log
*.fls
*.fdb_latexmk
*.out
*.bbl
*.blg
*.synctex.gz
*.toc
